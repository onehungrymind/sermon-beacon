- args:
    sql: CREATE TABLE "public"."media"("id" uuid NOT NULL, "url" text, "embedCode"
      text, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: media
    schema: public
  type: add_existing_table_or_view
