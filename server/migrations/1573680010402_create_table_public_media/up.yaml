- args:
    sql: CREATE TABLE "public"."media"("id" uuid NOT NULL DEFAULT gen_random_uuid(), "url" text, "embedCode"
      text, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: media
    schema: public
  type: add_existing_table_or_view
